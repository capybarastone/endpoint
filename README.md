# Herd endpoint agent

## Install
1. Install golang, then `sudo ./make.sh install`
2. Edit `/etc/herd/config.toml` to set server IP address
3. Profit (aka start `/usr/bin/herdagent` with your preferred service manager)